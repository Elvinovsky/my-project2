const sum = (a,b) => a + b 

const myName = () => {
    console.log('Elvin')
}

export {
    sum, 
    myName
}
//  'export default sum' <-- это экспорт по умолчанию позваляет экспортировать только одну переменнуюю

